export interface DebugInfos {
  source: string // Source code
  filename: string // Source file name
  pos: number // Position of the corresponding token in the source code
  length: number // Length of the corresponding token in the code
}
